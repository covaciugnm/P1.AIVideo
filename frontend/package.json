{
  "name": "aivideo-frontend",
  "version": "0.0.1",
  "private": true,
  "description": "P1.AIVideo frontend (Next.js). Phase 0 scaffold; real app lands in Phase 4.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "echo \"Phase 4 will replace this with: next dev\" && exit 0",
    "build": "echo \"Phase 4 will replace this with: next build\" && exit 0",
    "start": "echo \"Phase 4 will replace this with: next start\" && exit 0",
    "lint": "echo \"Phase 4 will add an eslint config\" && exit 0",
    "typecheck": "echo \"Phase 4 will add tsconfig + typecheck\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
