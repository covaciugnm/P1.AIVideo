# F5TTS-Ro wrapper — minimal API layer.
# Heavy deps (torch, f5-tts) are imported lazily inside the request handler;
# the wrapper reports runtime_missing if they aren't installed. Operators can
# rebuild this image with --build-arg INSTALL_F5TTS=true (future) or extend
# the base layer to pin a specific torch/cuda build.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
