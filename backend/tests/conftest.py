# Phase 0 scaffold. Shared pytest fixtures land in Phase 1 alongside the
# backend application code. Keeping this file empty (but present) so pytest
# can discover the tests/ root once tests start landing.
